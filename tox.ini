# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py39

[testenv]
deps =
    # list here any dependence needed for tests
commands =
    # you can run any command line tool here - not just tests
    python -m unittest discover
